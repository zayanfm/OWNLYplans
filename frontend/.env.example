# Optional. Expo Go automatically uses Metro's LAN host during development.
# Set this for tunnel mode, deployed APIs, or a non-standard backend address.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:5000
